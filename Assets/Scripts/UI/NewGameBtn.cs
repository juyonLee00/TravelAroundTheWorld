using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewGameBtn : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void OnClick()
    {
        /*
        if(saveDataLength < maxSaveDataNum)
        {
            새 게임 시작
            튜토리얼 씬으로 이동.
            SceneManager.LoadScene("TutorialScene");
        }

        else
        {
        //UIManager에서 팝업윈도우 보여주는 함수 ShowPopupWindow()구현
        //overloading
        // arg : null = "에러가 발생했습니다."
        // arg : string = Dictionary<string, string>()에서 맞는 string값 찾기
            UIManager.ShowPopupWindow(dataIsFullText);
        }
        */
    }
}
