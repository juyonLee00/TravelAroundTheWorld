using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoadGameBtn : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void OnClick()
    {
        /*
         if(saveDataLength != 0)
        {
        //UIManager에서 팝업윈도우 보여주는 함수 ShowPopupWindow()구현
        //overloading
        // arg : null = "에러가 발생했습니다."
        // arg : string = Dictionary<string, string>()에서 맞는 string값 찾기
            UIManager.ShowPopupWindow(dataIsEmpty);
        }
        else
        {
        //UIManager에서 원하는 UI 띄워주는 SetActiveUI()추가
        // arg : string
        // WindowDataGroup : Dictionary<string, T>()
            UIManager.SetActiveUI(SaveDataUI);
        }
         */
    }
}
